---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusters.kubernetes.civo.upbound.io
spec:
  group: kubernetes.civo.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - civo
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Cluster is the Schema for the Clusters API. Provides a Civo Kubernetes
          cluster resource. This can be used to create, delete, and modify clusters.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec defines the desired state of Cluster
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  applications:
                    description: |-
                      sensitive; the available applications can be listed with the Civo CLI: 'civo kubernetes applications ls'. If you want to remove a default installed application, prefix it with a '-', e.g. -Traefik. For application that supports plans, you can use 'app_name:app_plan' format e.g. 'Linkerd:Linkerd & Jaeger' or 'MariaDB:5GB'. View list of apps on the Civo CLI --> civo kubernetes apps ls
                      Comma separated list of applications to install. Spaces within application names are fine, but shouldn't be either side of the comma. Application names are case-sensitive; the available applications can be listed with the Civo CLI: 'civo kubernetes applications ls'. If you want to remove a default installed application, prefix it with a '-', e.g. -Traefik. For application that supports plans, you can use 'app_name:app_plan' format e.g. 'Linkerd:Linkerd & Jaeger' or 'MariaDB:5GB'.
                    type: string
                  clusterType:
                    description: |-
                      (String) The type of cluster to create, valid options are k3s or talos the default is k3s
                      The type of cluster to create, valid options are `k3s` or `talos` the default is `k3s`
                    type: string
                  cni:
                    description: |-
                      (String) The cni for the k3s to install (the default is flannel) valid options are cilium or flannel
                      The cni for the k3s to install (the default is `flannel`) valid options are `cilium` or `flannel`
                    type: string
                  firewallId:
                    description: |-
                      (String) The existing firewall ID to use for this cluster
                      The existing firewall ID to use for this cluster
                    type: string
                  firewallIdRef:
                    description: Reference to a Firewall in vpc to populate firewallId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  firewallIdSelector:
                    description: Selector for a Firewall in vpc to populate firewallId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  kubernetesVersion:
                    description: |-
                      (String) The version of k3s to install
                      The version of k3s to install (optional, the default is currently the latest stable available)
                    type: string
                  name:
                    description: |-
                      (String) Name for your cluster, must be unique within your account
                      Name for your cluster, must be unique within your account
                    type: string
                  networkId:
                    description: |-
                      (String) The network for the cluster, if not declare we use the default one
                      The network for the cluster, if not declare we use the default one
                    type: string
                  networkIdRef:
                    description: Reference to a Network in vpc to populate networkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkIdSelector:
                    description: Selector for a Network in vpc to populate networkId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  numTargetNodes:
                    description: |-
                      (Number, Deprecated) The number of instances to create
                      The number of instances to create (optional, the default at the time of writing is 3)
                    format: int64
                    type: integer
                  pools:
                    description: '(Block List, Min: 1, Max: 1) (see below for nested
                      schema)'
                    properties:
                      label:
                        description: |-
                          (String) Node pool label, if you don't provide one, we will generate one for you
                          Node pool label, if you don't provide one, we will generate one for you
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: (Map of String)
                        type: object
                        x-kubernetes-map-type: granular
                      nodeCount:
                        description: |-
                          (Number) Number of nodes in the nodepool
                          Number of nodes in the nodepool
                        format: int64
                        type: integer
                      publicIpNodePool:
                        description: |-
                          (Boolean) Node pool belongs to the public ip node pool
                          Node pool belongs to the public ip node pool
                        type: boolean
                      size:
                        description: |-
                          -> civo kubernetes size
                          Size of the nodes in the nodepool
                        type: string
                      taint:
                        description: (Block Set) (see below for nested schema)
                        items:
                          properties:
                            effect:
                              description: (String)
                              type: string
                            key:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                    type: object
                  region:
                    description: |-
                      (String) The region for the cluster, if not declare we use the region in declared in the provider
                      The region for the cluster, if not declare we use the region in declared in the provider
                    type: string
                  tags:
                    description: |-
                      (String) Space separated list of tags, to be used freely as required
                      Space separated list of tags, to be used freely as required
                    type: string
                  targetNodesSize:
                    description: |-
                      (String, Deprecated) The size of each node
                      The size of each node (optional, the default is currently g4s.kube.medium)
                    type: string
                  volumeType:
                    description: The volume type used for the kubernetes nodes
                    type: string
                  writeKubeconfig:
                    description: |-
                      (boolean) (false by default) when set to true, kubeconfig is saved to the provider state file
                      whether to write the kubeconfig to state
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  applications:
                    description: |-
                      sensitive; the available applications can be listed with the Civo CLI: 'civo kubernetes applications ls'. If you want to remove a default installed application, prefix it with a '-', e.g. -Traefik. For application that supports plans, you can use 'app_name:app_plan' format e.g. 'Linkerd:Linkerd & Jaeger' or 'MariaDB:5GB'. View list of apps on the Civo CLI --> civo kubernetes apps ls
                      Comma separated list of applications to install. Spaces within application names are fine, but shouldn't be either side of the comma. Application names are case-sensitive; the available applications can be listed with the Civo CLI: 'civo kubernetes applications ls'. If you want to remove a default installed application, prefix it with a '-', e.g. -Traefik. For application that supports plans, you can use 'app_name:app_plan' format e.g. 'Linkerd:Linkerd & Jaeger' or 'MariaDB:5GB'.
                    type: string
                  clusterType:
                    description: |-
                      (String) The type of cluster to create, valid options are k3s or talos the default is k3s
                      The type of cluster to create, valid options are `k3s` or `talos` the default is `k3s`
                    type: string
                  cni:
                    description: |-
                      (String) The cni for the k3s to install (the default is flannel) valid options are cilium or flannel
                      The cni for the k3s to install (the default is `flannel`) valid options are `cilium` or `flannel`
                    type: string
                  firewallId:
                    description: |-
                      (String) The existing firewall ID to use for this cluster
                      The existing firewall ID to use for this cluster
                    type: string
                  firewallIdRef:
                    description: Reference to a Firewall in vpc to populate firewallId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  firewallIdSelector:
                    description: Selector for a Firewall in vpc to populate firewallId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  kubernetesVersion:
                    description: |-
                      (String) The version of k3s to install
                      The version of k3s to install (optional, the default is currently the latest stable available)
                    type: string
                  name:
                    description: |-
                      (String) Name for your cluster, must be unique within your account
                      Name for your cluster, must be unique within your account
                    type: string
                  networkId:
                    description: |-
                      (String) The network for the cluster, if not declare we use the default one
                      The network for the cluster, if not declare we use the default one
                    type: string
                  networkIdRef:
                    description: Reference to a Network in vpc to populate networkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkIdSelector:
                    description: Selector for a Network in vpc to populate networkId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  numTargetNodes:
                    description: |-
                      (Number, Deprecated) The number of instances to create
                      The number of instances to create (optional, the default at the time of writing is 3)
                    format: int64
                    type: integer
                  pools:
                    description: '(Block List, Min: 1, Max: 1) (see below for nested
                      schema)'
                    properties:
                      label:
                        description: |-
                          (String) Node pool label, if you don't provide one, we will generate one for you
                          Node pool label, if you don't provide one, we will generate one for you
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: (Map of String)
                        type: object
                        x-kubernetes-map-type: granular
                      nodeCount:
                        description: |-
                          (Number) Number of nodes in the nodepool
                          Number of nodes in the nodepool
                        format: int64
                        type: integer
                      publicIpNodePool:
                        description: |-
                          (Boolean) Node pool belongs to the public ip node pool
                          Node pool belongs to the public ip node pool
                        type: boolean
                      size:
                        description: |-
                          -> civo kubernetes size
                          Size of the nodes in the nodepool
                        type: string
                      taint:
                        description: (Block Set) (see below for nested schema)
                        items:
                          properties:
                            effect:
                              description: (String)
                              type: string
                            key:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                    type: object
                  region:
                    description: |-
                      (String) The region for the cluster, if not declare we use the region in declared in the provider
                      The region for the cluster, if not declare we use the region in declared in the provider
                    type: string
                  tags:
                    description: |-
                      (String) Space separated list of tags, to be used freely as required
                      Space separated list of tags, to be used freely as required
                    type: string
                  targetNodesSize:
                    description: |-
                      (String, Deprecated) The size of each node
                      The size of each node (optional, the default is currently g4s.kube.medium)
                    type: string
                  volumeType:
                    description: The volume type used for the kubernetes nodes
                    type: string
                  writeKubeconfig:
                    description: |-
                      (boolean) (false by default) when set to true, kubeconfig is saved to the provider state file
                      whether to write the kubeconfig to state
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.pools is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.pools)
                || (has(self.initProvider) && has(self.initProvider.pools))'
          status:
            description: ClusterStatus defines the observed state of Cluster.
            properties:
              atProvider:
                properties:
                  apiEndpoint:
                    description: |-
                      (String) The API server endpoint of the cluster
                      The API server endpoint of the cluster
                    type: string
                  applications:
                    description: |-
                      sensitive; the available applications can be listed with the Civo CLI: 'civo kubernetes applications ls'. If you want to remove a default installed application, prefix it with a '-', e.g. -Traefik. For application that supports plans, you can use 'app_name:app_plan' format e.g. 'Linkerd:Linkerd & Jaeger' or 'MariaDB:5GB'. View list of apps on the Civo CLI --> civo kubernetes apps ls
                      Comma separated list of applications to install. Spaces within application names are fine, but shouldn't be either side of the comma. Application names are case-sensitive; the available applications can be listed with the Civo CLI: 'civo kubernetes applications ls'. If you want to remove a default installed application, prefix it with a '-', e.g. -Traefik. For application that supports plans, you can use 'app_name:app_plan' format e.g. 'Linkerd:Linkerd & Jaeger' or 'MariaDB:5GB'.
                    type: string
                  clusterType:
                    description: |-
                      (String) The type of cluster to create, valid options are k3s or talos the default is k3s
                      The type of cluster to create, valid options are `k3s` or `talos` the default is `k3s`
                    type: string
                  cni:
                    description: |-
                      (String) The cni for the k3s to install (the default is flannel) valid options are cilium or flannel
                      The cni for the k3s to install (the default is `flannel`) valid options are `cilium` or `flannel`
                    type: string
                  createdAt:
                    description: |-
                      (String) The timestamp when the cluster was created
                      The timestamp when the cluster was created
                    type: string
                  dnsEntry:
                    description: |-
                      (String) The DNS name of the cluster
                      The DNS name of the cluster
                    type: string
                  firewallId:
                    description: |-
                      (String) The existing firewall ID to use for this cluster
                      The existing firewall ID to use for this cluster
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  installedApplications:
                    description: (List of Object) (see below for nested schema)
                    items:
                      properties:
                        application:
                          description: |-
                            (String) name of the application
                            Name of application
                          type: string
                        category:
                          description: |-
                            (String) category of the application
                            Category of the application
                          type: string
                        installed:
                          description: |-
                            (Boolean) whether application is installed or not
                            Application installation status (`true` if installed)
                          type: boolean
                        version:
                          description: |-
                            (String) version of the application
                            Version of application
                          type: string
                      type: object
                    type: array
                  kubernetesVersion:
                    description: |-
                      (String) The version of k3s to install
                      The version of k3s to install (optional, the default is currently the latest stable available)
                    type: string
                  masterIp:
                    description: |-
                      (String) The IP address of the master node
                      The IP address of the master node
                    type: string
                  name:
                    description: |-
                      (String) Name for your cluster, must be unique within your account
                      Name for your cluster, must be unique within your account
                    type: string
                  networkId:
                    description: |-
                      (String) The network for the cluster, if not declare we use the default one
                      The network for the cluster, if not declare we use the default one
                    type: string
                  numTargetNodes:
                    description: |-
                      (Number, Deprecated) The number of instances to create
                      The number of instances to create (optional, the default at the time of writing is 3)
                    format: int64
                    type: integer
                  pools:
                    description: '(Block List, Min: 1, Max: 1) (see below for nested
                      schema)'
                    properties:
                      instanceNames:
                        description: |-
                          (List of String) Instance names in the nodepool
                          Instance names in the nodepool
                        items:
                          type: string
                        type: array
                      label:
                        description: |-
                          (String) Node pool label, if you don't provide one, we will generate one for you
                          Node pool label, if you don't provide one, we will generate one for you
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: (Map of String)
                        type: object
                        x-kubernetes-map-type: granular
                      nodeCount:
                        description: |-
                          (Number) Number of nodes in the nodepool
                          Number of nodes in the nodepool
                        format: int64
                        type: integer
                      publicIpNodePool:
                        description: |-
                          (Boolean) Node pool belongs to the public ip node pool
                          Node pool belongs to the public ip node pool
                        type: boolean
                      size:
                        description: |-
                          -> civo kubernetes size
                          Size of the nodes in the nodepool
                        type: string
                      taint:
                        description: (Block Set) (see below for nested schema)
                        items:
                          properties:
                            effect:
                              description: (String)
                              type: string
                            key:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                    type: object
                  ready:
                    description: |-
                      (Boolean) When cluster is ready, this will return true
                      When cluster is ready, this will return `true`
                    type: boolean
                  region:
                    description: |-
                      (String) The region for the cluster, if not declare we use the region in declared in the provider
                      The region for the cluster, if not declare we use the region in declared in the provider
                    type: string
                  status:
                    description: |-
                      (String) Status of the cluster
                      Status of the cluster
                    type: string
                  tags:
                    description: |-
                      (String) Space separated list of tags, to be used freely as required
                      Space separated list of tags, to be used freely as required
                    type: string
                  targetNodesSize:
                    description: |-
                      (String, Deprecated) The size of each node
                      The size of each node (optional, the default is currently g4s.kube.medium)
                    type: string
                  volumeType:
                    description: The volume type used for the kubernetes nodes
                    type: string
                  writeKubeconfig:
                    description: |-
                      (boolean) (false by default) when set to true, kubeconfig is saved to the provider state file
                      whether to write the kubeconfig to state
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
