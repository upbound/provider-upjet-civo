apiVersion: v1
kind: Secret
metadata:
  name: civo-creds
  # For the namespaced ProviderConfig the Secret must live in the same
  # namespace as the managed resources that reference it.
  namespace: upbound-system
type: Opaque
stringData:
  creds: |
    {
      "token": "<Civo API token>"
    }
